# Google Play release

The iOS side is written up in `app-store.md`; this covers only what Android needs on top.
Nothing here is done yet beyond what the repo already carries.

## Identifiers

| Item | Value |
| --- | --- |
| Package | `pt.jogarbumi.app` |
| Version source | `appVersionSource: remote` in `eas.json` — EAS owns the version code |
| Google Cloud project | `606345526586` (`jarvis-485711`), the same one the web and iOS clients live in |

## What the repo already has

- `android.package`, set alongside the adaptive icon layers in `app.json`
- foreground, background and monochrome icons, all generated by `npm run generate-icons`
  from the same geometry as everything else
- `assets/images/play-feature-graphic.png` — 1024x500, no alpha, which Play requires
- a privacy policy at `https://www.jogarbumi.pt/privacidade`, in three languages, already
  covering the friends board and the notification token

## Blocked on, in order

1. **An Android OAuth client.** Google sign-in on Android checks the app's signing certificate,
   so the client needs the SHA-1 of the keystore EAS generated. `npx eas-cli credentials
   --platform android` prints it; the client is created in the same Cloud project as the
   others, under Credentials > OAuth client ID > Android. Without this, sign-in fails on
   Android only — the rest of the app works, which makes it easy to miss.
2. **FCM credentials**, if the friends notification is to arrive on Android. Firebase project,
   `google-services.json` into the repo, and the FCM v1 service account key uploaded to EAS.
   The daily reminder is scheduled on the device and needs none of this.
3. **A Play service account** for `eas submit` to upload without a browser: Play Console >
   Setup > API access, a service account in Google Cloud with the Release Manager role, and
   its JSON key referenced from `eas.json` under `submit.production.android`.
4. **Store listing.** Title, short description (80 characters), full description (4000), the
   feature graphic above, at least two phone screenshots, and a category. The three
   descriptions are already written in `app-store.md` under the localised listings and read
   the same on Play.
5. **Data safety form.** The same answers as Apple's privacy questionnaire: account name and
   email from sign-in, game progress linked to identity, no tracking, no advertising. The
   friends board shares progress between players and never a name, email or account id.
6. **Content rating questionnaire.** A logic puzzle with no ads, no purchases and no user
   content; it lands at the lowest rating in every region.

## Before the first build

`npx eas-cli build --profile production --platform android` produces an `.aab`, which is what
Play takes. It works today — the Android OAuth client is not needed to *build*, only to sign
in — so an internal-testing upload can happen before item 1 is done, as long as whoever tests
it knows sign-in will fail until it is.

## Verify in the Play Console before promising a date

Google requires personal developer accounts opened after November 2023 to run a closed test
with a minimum number of testers for a continuous period before production access is granted.
Both the count and the duration have changed more than once, and they are stated in the
console against this specific account — read them there rather than trusting a number written
here. It is the item most likely to decide when the app can actually ship.
